---
import Default from "./Default.astro";
import Whitehead from "./WhiteHead.astro";
import type BoxProps from "../../navigation.ts";

type Props = BoxProps;
const { type: BoxTag = Default, ...props } = Astro.props;
---

<BoxTag {...props}>
  <slot />
</BoxTag>
