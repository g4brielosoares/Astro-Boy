---
const { id } = Astro.props;
import SlideNumber from "../components/SlideNumber.astro";
---

<div
  class="relative w-full bg-[#20132CAA] p-6 md:p-8 rounded-2xl flex flex-col gap-5 text-white border-2 border-[#DE386B]"
>
  {id && <SlideNumber>Slide {id}</SlideNumber>}
  <slot />
</div>
